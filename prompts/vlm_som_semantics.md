这是一张带有数字编号（红框和数字ID）的软件/网页界面截图。
请仔细观察图中的每一个数字标号，并告诉我它们对应的功能、图标或内容是什么。
注意标号id是框的左上角的数字
【极其严格的输出规则】
1. 必须且只能输出一个合法的 JSON 字典，绝对不要包含任何解释性文字。
2. JSON 的键（Key）必须是图中的数字 ID（字符串格式，例如 "1", "15"）。
3. JSON 的值（Value）必须是该元素极其简短的语义描述（例如 "搜索框", "点赞图标", "用户头像"）。
4. 尽量把你能够看清的 ID 全部列出来。

【输出示例】
{
    "1": "返回上一页",
    "2": "浏览器地址栏",
    "15": "购物车图标",
    "22": "确认提交按钮"
}