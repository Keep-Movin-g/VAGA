你是一个资深的 UI 布局分析专家。
请分析这张屏幕截图，将屏幕上【可见的实体元素（如图标、窗口、任务栏、文本等）】划分为 3 到 6 个最主要的宏观功能区块。

【严格规则】
1. **只关注实体，无视背景**：你只需要框选有实际内容（图标、文字、控件）的密集区域。**绝对不要**去框选纯净的桌面壁纸或无内容的空白背景！
2. **绝对宏观（防碎片化）**：严禁为单个图标或按钮划分区域！必须将相邻的同类元素打包成一个巨大的“板块”。例如：屏幕左侧有几十个快捷方式，必须用一个大框将它们【全部】包进去，命名为“左侧桌面快捷方式区”。
3. 输出边界框坐标 [x_min, y_min, x_max, y_max]，请使用 0.0 到 1.0 的相对比例坐标。
4. 必须且只能输出合法的 JSON 格式，绝对不要包含任何解释性文字。

【输出格式示例】
{
  "左侧桌面快捷方式区": [0.0, 0.0, 0.15, 0.95],
  "右上角系统监控悬浮窗": [0.80, 0.0, 1.0, 0.20],
  "底部系统任务栏": [0.0, 0.95, 1.0, 1.0]
}